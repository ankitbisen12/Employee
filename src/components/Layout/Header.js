import classes from "./Header.module.css";

const Header = () => {
  return (
    <header className={classes.header}>
      <i class="fa-solid fa-code"> Pre-Employment Assignment</i>
    </header>
  );
};

export default Header;
